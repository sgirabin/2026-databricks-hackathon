# Optional keys/configuration. The app runs with public data.gov.sg + OneMap search without most keys.
DATA_GOV_API_KEY=

# OneMap public search endpoint works without credentials for geocoding in this prototype.
# Add credentials later if you use newer authenticated endpoints or higher-volume API access.
ONEMAP_EMAIL=
ONEMAP_PASSWORD=
ONEMAP_ACCESS_TOKEN=

# LTA DataMall is required for live bus stops / bus arrival.
# Register with LTA DataMall and set this in Databricks App environment variables.
LTA_ACCOUNT_KEY=

# Optional evidence / promotion / event search. Without this, the app shows source-backed manual links only.
BING_SEARCH_KEY=
BING_SEARCH_ENDPOINT=https://api.bing.microsoft.com/v7.0/search

# Optional Databricks AI / SQL hooks.
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_MODEL_ENDPOINT=databricks-meta-llama-3-3-70b-instruct
DATABRICKS_SERVER_HOSTNAME=
DATABRICKS_HTTP_PATH=
DATABRICKS_SQL_ACCESS_TOKEN=
USE_DATABRICKS_SQL=false

# Optional Lakebase / Postgres memory for saved blocks, interests, watchlists and alerts.
LAKEBASE_DATABASE_URL=

GOAROUND_CATALOG=main
GOAROUND_SCHEMA=goaround_sg
GOAROUND_CACHE_DIR=data/cache

# Backward-compatible names used by older notebooks.
HOMEWISE_CATALOG=main
HOMEWISE_SCHEMA=homewise_sg
HOMEWISE_CACHE_DIR=data/cache
